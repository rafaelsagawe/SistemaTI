@model IEnumerable<SistemaTI.Models.Especificacao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fabicante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Modelo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fabicante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Modelo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EspecificacaoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EspecificacaoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EspecificacaoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
